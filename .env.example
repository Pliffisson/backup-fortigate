# ===================================================================
# FORTIGATE BACKUP SYSTEM - CONFIGURAÇÃO DE AMBIENTE
# ===================================================================
# Este arquivo contém todas as variáveis de ambiente necessárias
# para o funcionamento do sistema de backup FortiGate.
# Copie este arquivo para .env e configure suas credenciais.
# ===================================================================

# === AGENDAMENTO AUTOMÁTICO ===
# O sistema utiliza scheduler Python integrado (sem dependência de cron)
# Formato: minuto hora dia mês dia_da_semana
# Exemplos:
#   Diário às 02:00: 0 2 * * *
#   A cada 6 horas: 0 */6 * * *
#   Segunda a sexta às 08:00: 0 8 * * 1-5
#   Semanal (domingo às 03:00): 0 3 * * 0
#   Duas vezes por dia: 0 9,21 * * *
CRON_SCHEDULE=0 2 * * *

# === NOTIFICAÇÕES TELEGRAM (OPCIONAL) ===
# Para receber notificações dos backups via Telegram
# 1. Crie um bot com @BotFather
# 2. Obtenha o token do bot
# 3. Obtenha o chat_id enviando uma mensagem e acessando:
#    https://api.telegram.org/bot<TOKEN>/getUpdates
TELEGRAM_BOT_TOKEN=seu_token_aqui
TELEGRAM_CHAT_ID=seu_chat_id_aqui

# === CONFIGURAÇÕES DE BACKUP ===
# Quantos dias manter os backups (limpeza automática)
BACKUP_RETENTION_DAYS=30

# Coletar informações adicionais do sistema (show system status)
COLLECT_SYSTEM_INFO=true

# Formato dos arquivos de backup (text recomendado)
BACKUP_FORMAT=text

# === CONFIGURAÇÕES SSH ===
# Timeout para conexões SSH em segundos
SSH_TIMEOUT=30

# === CONFIGURAÇÕES DE LOG ===
# Nível de log: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Salvar logs em arquivo (recomendado: true)
LOG_TO_FILE=true

# === CONFIGURAÇÕES DE FUSO HORÁRIO (OPCIONAL) ===
# Descomente e configure se necessário
# TZ=America/Sao_Paulo
# TZ=America/New_York
# TZ=Europe/London

# === DIRETÓRIOS DO SISTEMA (NÃO ALTERAR) ===
# Estes diretórios são mapeados pelos volumes Docker
BACKUP_DIR=/app/backups
LOG_DIR=/app/logs
CONFIG_DIR=/app/config

# ===================================================================
# PRÓXIMOS PASSOS:
# 1. Configure os dispositivos em config/devices.json
# 2. Execute: docker compose up -d --build
# 3. Verifique: docker compose exec fortigate-backup ./scripts/manage-internal-cron.sh status
# ===================================================================